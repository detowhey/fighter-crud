export class FightRecord {

    win: number;
    loose: number;
    draw: number;

    constructor(win: number, loose: number, draw: number) {
        this.win = win;
        this.loose = loose;
        this.draw = draw;
    }
}
